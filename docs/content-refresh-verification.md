# 内容更新验证记录

验证完成：2026-09-17（北京时间）。内容和简历于 2026-09-16 同步。

## 范围

工作目录：`C:/tmp/xuzheee.github.io-content-refresh`
分支：`codex/content-refresh-20260916`
基线：`af79442`
提交状态：等待用户确认；没有创建 commit、推送或部署。

保留首页六个栏目与顺序、侧栏、四类项目总览、现有视觉样式、博客和兴趣页。增量补充网易与独立 Inky Paper，更新 URAI、CV、下载简历和共享事实，增加三个站内案例。

## 已通过

- Astro 静态构建：13 个页面。使用桌面运行环境的 Node 24.19.0 执行 `node_modules/astro/astro.js build`，未升级依赖。
- `scripts/check-content.mjs`：首页恰好三个指定案例、首页无 HV、总览保留十个原项目并新增两个、每篇七段结构、政务数量与任职日期、网易 CV/案例结果及限定一致、240 个内部资源引用、PDF 构建完整性和 RSS。
- 最新简历文件与 `public/resume.pdf` 的 SHA-256 相同：`D0440B3CFA671B6B56F3C70023891463AE5C11100F3D0A4BCB267CFBE96DBA39`。预览下载地址返回 HTTP 200、`application/pdf`、8,301,209 字节。
- Chrome 实际点击首页“阅读案例”进入网易案例；手机菜单进入项目总览后正常收起，总览显示 12 个项目。
- 首页、项目总览、CV、网易案例、Paper 案例、URAI 案例，在 320、390、1440px 宽度下共 18 组页面检查通过，无横向溢出。
- 已查看首页桌面、手机首屏、网易案例桌面与 CV 截图；Paper 截图滚动进入可视区后正常加载。
- `git diff --check` 通过。原 main 工作区仅保留上一轮未跟踪的规划文档，页面未改。

## 检查中修复

- Paper 纵向截图的最大宽度在 320px 屏幕上导致 24px 横向溢出；增加随容器缩放的宽度，已通过上述三种尺寸检查。
- 构建发现原 RSS 使用小写 `get`，当前 Astro 未生成有效响应；改为 `GET`，输出有效 feed。
- 没有外链的项目图片取消 `#` 占位跳转；站内案例同页导航，外链保留新窗口。

## 证据与边界

浏览截图位于本机 `output/playwright/`，由 Git 忽略：`home-desktop.png`、`home-mobile-top.png`、`netease-desktop.png`、`netease-mobile.png`、`cv-desktop.png`、`paper-mobile.png`、`paper-mobile-task.png`。

本轮验证网站内容、构建、资源与浏览行为。没有重新分析网易业务数据，没有重跑 Inky 原生应用和模型验证，也没有逐个验收历史外部 Demo。浏览器兼容性仅覆盖本次 Chrome；构建提示现有 Browserslist 数据较旧，未顺带升级依赖。

本地预览：`http://127.0.0.1:4327/`。这是独立 worktree 的静态构建预览，不是线上发布。服务关闭后可在此目录运行 `npm run preview -- --host 127.0.0.1 --port 4327` 重开。

## 2026-09-17 集钉补充

- 总览增至 13 个项目，首页仍为原定 3 个案例。
- 用户确认职责为合作开发中的产品设计、小程序开发与测试。
- 核对原始证书与截图；证书归属香港中文大学（深圳），登记号 2026SR0945487。
- 构建与内容检查通过，243 个内部资源有效；浏览器确认口令、职责、证书入口和截图展开正常。
- 未实际验证微信小程序采集、提交或导出流程。
